pluginManagement {
    repositories {
        // fabricloom 原版的仓库
        maven {
            name = 'Fabric'
            url = 'https://maven.fabricmc.net/'
        }

        // 其余插件所存放的仓库
        gradlePluginPortal()
    }
}

include 'common', 'with_blockus'